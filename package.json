{
  "name": "delivery-agent-chat-system-v2",
  "version": "2.0.0",
  "description": "Real-time delivery agent chat system with location tracking, ETA, and notifications",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "test": "echo \"No tests yet\" && exit 0",
    "install-all": "npm install && cd client && npm install"
  },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "haversine-distance": "^1.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}